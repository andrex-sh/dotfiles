# Let Sixel graphics (e.g. yazi's image previews) pass through to the outer
# terminal instead of being filtered out. Requires tmux >= 3.4; foot is one
# of the terminals this is confirmed to work with.
set -g allow-passthrough on
